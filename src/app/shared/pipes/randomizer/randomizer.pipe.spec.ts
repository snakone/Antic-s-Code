import { RandomizerPipe } from '../randomizer.pipe';

describe('RandomizerPipe', () => {
  it('create an instance', () => {
    const pipe = new RandomizerPipe();
    expect(pipe).toBeTruthy();
  });
});
