import { StickyBoxDirective } from './sticky-box.directive';

describe('StickyBoxDirective', () => {
  it('should create an instance', () => {
    const directive = new StickyBoxDirective();
    expect(directive).toBeTruthy();
  });
});
